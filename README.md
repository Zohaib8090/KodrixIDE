# Kodrix IDE — `libs` Branch (Migrated)

⚠️ **This registry has been migrated to a dedicated separate repository:**
👉 **[Zohaib8090/KodrixMarketplace](https://github.com/Zohaib8090/KodrixMarketplace)**

All marketplace extensions, Node.js tool versions, registries (`versions.json`, `extensions.json`, `binaries.json`), and hosted assets now reside in the new `KodrixMarketplace` repository.

Please make any updates directly to that repository instead.
